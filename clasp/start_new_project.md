```bash
# install clasp package
npm install @google/clasp -g

# this package tells us gas functions
npm install @types/google-apps-script

# after execute above two command, package.json is generated and packages are listed in dependencies sections.


# create new clasp project
clasp create

# choose whatever your designate type like standalone

# push local files and compile to .gas code
clasp push

```
