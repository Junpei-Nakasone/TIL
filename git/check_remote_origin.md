To find out which remote branch url that local branch is tracking, execute below command.(only remote url)

```bash
$ git config --get remote.origin.url
```

execute below command to fetch full output.

```bash
git remote show origin
```
